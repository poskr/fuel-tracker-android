name: Build & Release APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      # 1) Клонируем репо (с вашим buildozer.spec и исходниками)
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2) Распаковываем app.zip — если вы всё ещё им пользуетесь
      - name: Extract app.zip (optional)
        run: |
          if [ -f "app.zip" ]; then
            echo "📦 app.zip found, extracting..."
            unzip -o app.zip -d src
            mv src/* .
          else
            echo "⚠️ app.zip not found, using repo sources."
          fi

      # 3) Собираем APK внутри Docker-образа с Buildozer
      - name: Build APK with Buildozer
        uses: ArtemSBulgakov/buildozer-action@v1
        id: buildozer
        with:
          workdir: .                 # тут лежит ваш buildozer.spec
          buildozer_version: stable  # или конкретная версия, например 1.2.0

      # 4) Загружаем APK как артефакт
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: FuelTracker-APK
          path: ${{ steps.buildozer.outputs.filename }}
